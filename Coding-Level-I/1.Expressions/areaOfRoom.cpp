#include <bits/stdc++.h>
using namespace std;
int main(){
    float width,length;
    cout << "Enter width(in feet): ";
    cin >> width;
    cout << "Enter length(in feet): ";
    cin >> length;
    double area = width * length;
    cout << "The area is: " << area << " ";
    return 0;
}